# Leave unset to run against the built-in mock engine (src/mock) — good for
# building/demoing the UI before Person A's backend exists.
# Once the backend is up, point this at it and everything switches over
# with no other code changes (see src/hooks/useRuleMeshSocket.ts).
VITE_WS_URL=ws://localhost:8000/ws/live
